package com.ashcroft.ripple.core.simulation

/**
 * Phase 2+ home of the deterministic, event-driven simulation engine: the
 * seeded clock, scheduled-action priority queue, fidelity tiers and
 * variable-frequency updates described in the design.
 *
 * Deliberately empty in Phase 1 — the module boundary exists so later phases
 * have a clear place to land without reshaping the build. No simulation
 * content is implemented yet.
 */
internal object SimulationModulePlaceholder
